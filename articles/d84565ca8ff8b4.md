---
title: Typescriptについて初心者が学習してみる
emoji: 📑
type: tech
topics: ["typescript", "初心者"]
published: true
---

# Typescriptの学習を開始した経緯
JavascriptとTypescriptの違いについてあまり詳しくなかった（型変換が加わっていること）くらいしか詳しくなかったので、本格的に学習してみようと思います。

# 公式ドキュメントを確認
まずTypescriptの公式ドキュメントをチェックすることにしました。
https://www.typescriptlang.org/docs/handbook/intro.html

## Typescriptの目的
Typescriptは静的な型チェッカーであり、プログラム実行前に静的に型チェックが実施されます。
これにより、型エラーやタイプミス、実行時の誤った判定などを防げるそうです。
少しイメージがもちにくかったので、GPT5にどのような場面なのか具体的にしてもらいます。

### 1.JavaScriptでの問題例（TypeScript未導入）
例：関数の引数に「数値」を期待しているのに「文字列」が渡される
```js
// JavaScript（型がない）
function add(a, b) {
  return a + b;
}

console.log(add(3, 5));      // 8（正しい）
console.log(add("3", 5));    // "35"（間違い：文字列結合になってしまう）
```
🔍問題点
add関数は「数値を足す関数」なのに、文字列を渡してもエラーにならない。
JavaScriptは動的型付け言語のため、
実行して初めて「意図と違う動作（文字列結合）」が発覚します。

### 2.TypeScriptを導入した場合の同じコード
```ts
// TypeScript（静的型チェックあり）
function add(a: number, b: number): number {
  return a + b;
}

console.log(add(3, 5));      // OK
console.log(add("3", 5));    // ❌ エラー：
                             // Argument of type 'string' is not assignable to parameter of type 'number'
```
💡 違い
実行前（コンパイル時）に、**"string型をnumberに渡している"**と警告が出る。
実際にアプリを動かす前にバグを防げる。
チーム開発や大規模アプリでは特に有効。

### 3.よくある実例：APIレスポンスの型ミス
Javascript版
```js
// APIからユーザー情報を取得する
const user = await fetch("/api/user").then(res => res.json());

// 本来 user.name は string を想定していたが、実際は null かもしれない
console.log(user.name.toUpperCase()); 
// ❌ 実行時に「Cannot read properties of null」エラー
```

Typescript版
```ts
type User = {
  name: string | null;
};

const user: User = await fetch("/api/user").then(res => res.json());

// エラーを検出してくれる
console.log(user.name.toUpperCase()); // ❌ Object is possibly 'null'
```

修正例
```ts
if (user.name) {
  console.log(user.name.toUpperCase()); // OK
}
```
※if文でユーザー名が存在した場合のみconsole.logで出力する


さて、これで目的については理解できました。
プログラム初心者向けには以下どちらかの記事から学習を始めることが推奨されています。
https://developer.microsoft.com/javascript/
https://developer.mozilla.org/docs/Web/JavaScript/Guide


# THE BASIC(基礎)



